import React from 'react'

export default function users() {
  return (
    <div>
      <p>Have a page that shows you the user’s information:</p>
      <p>This is the page shown when a user is selected</p>
      <p>The page should list a user’s albums</p>
      <p>The page must run a GET request for the user-selected</p>
      <p>The page must run a GET request for a user’s albums</p>
    </div>
  )
}

 